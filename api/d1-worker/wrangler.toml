name = "meme-manager-d1-worker"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[[d1_databases]]
binding = "DB"
database_name = "meme-manager-db"
# 部署后用 `wrangler d1 create meme-manager-db` 创建数据库，将返回的 database_id 填入下方
database_id = "your-database-id-here"
