{
  "name": "meme-manager-d1-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init:local": "wrangler d1 execute meme-manager-db --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute meme-manager-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0",
    "@cloudflare/workers-types": "^4.0.0"
  }
}